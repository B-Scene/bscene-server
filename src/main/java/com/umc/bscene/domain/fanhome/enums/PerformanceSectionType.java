package com.umc.bscene.domain.fanhome.enums;

public enum PerformanceSectionType {
    UPCOMING,     // 다가오는 공연 (팔로우 밴드들의 실제 공연) — 헤더 "다가오는 공연"
    RECOMMENDED   // 추천 공연 — 헤더 "이런 공연은 어때요?" / "다가오는 공연이 없어요"
}
